---
- name: Converge
  hosts: all
  tasks:
    - name: "Before running set-variables"
      ansible.builtin.include_role:
        name: "set-variables"
    - name: "Include tool-docker"
      include_role:
        name: "tool-docker"
